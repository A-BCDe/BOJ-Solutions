#include <bitset>
#include <iostream>
#include <map>
using namespace std;
using bitfield = bitset<160>;
int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	int n, m, k;
	cin >> n >> m >> k;

}