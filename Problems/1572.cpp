#include <stdio.h>
#include <queue>
using namespace std;
struct comp { bool operator()(int a, int b) { return a > b; } };
int main() {
	int n, k;
	scanf("%d%d", &n, &k);
	for (int i = 0; i < n; i++) {

	}
}